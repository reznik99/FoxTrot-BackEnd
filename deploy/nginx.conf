# TODO: Fill in your server_name, domain, and any other customisations.
#
# TLS certs are managed by certbot on the host (systemd timer).
# Only this domain's fullchain.pem and privkey.pem are mounted in, at:
#   /etc/nginx/tls/fullchain.pem
#   /etc/nginx/tls/privkey.pem
#
# After certbot renews on the host, reload nginx:
#   docker compose exec nginx nginx -s reload

events {}

http {
    upstream api {
        server api:1234;
    }

    # TODO: Fill in your nginx config here.
}
